import React from "react";
import DefaultTemplate from "../components/templates/DefaultTemplate";

const HomePage = () => {
  return <DefaultTemplate>content here...</DefaultTemplate>;
};

export default HomePage;
